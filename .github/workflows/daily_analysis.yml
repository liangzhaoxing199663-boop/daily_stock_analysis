name: 'Daily Stock Analysis'

on:
  push:
    branches:
      - master
      - main
  schedule:
    # 每天 5 次运行
    - cron: '0 0 * * *'
    - cron: '0 5 * * *'
    - cron: '0 10 * * *'
    - cron: '30 12 * * *'
    - cron: '0 16 * * *'
  workflow_dispatch:

jobs:
  daily_analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          # 强制安装美股数据包
          pip install yfinance pandas_datareader requests urllib3 --upgrade --no-cache-dir

      # 👇👇👇【这里是重点】手术步骤 👇👇👇
      - name: 🔧 Force Replace Model Name (Magic Fix)
        run: |
          echo "正在执行代码手术..."
          # 找到所有文件里的 "gemini-3-flash-preview"，强行替换成 "gemini-1.5-flash"
          grep -rl "gemini-3-flash-preview" . | xargs sed -i 's/gemini-3-flash-preview/gemini-1.5-flash/g' || true
          
          # 以防万一，把备选模型也统一成 1.5-flash
          grep -rl "gemini-2.5-flash" . | xargs sed -i 's/gemini-2.5-flash/gemini-1.5-flash/g' || true
          echo "代码修改完成！现在所有模型都被强制锁定为 gemini-1.5-flash"

      - name: Run analysis
        env:
          LLM_PROVIDER: ${{ secrets.LLM_PROVIDER }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          STOCK_LIST: ${{ secrets.STOCK_LIST }}
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
          
          # 双重保险：环境变量也设为 1.5-flash
          LLM_MODEL: gemini-1.5-flash
          GOOGLE_API_KEY: ${{ secrets.LLM_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.LLM_API_KEY }}
          
        run: python main.py
