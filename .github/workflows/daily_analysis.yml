name: 'Daily Stock Analysis'

on:
  push:
    branches:
      - master
      - main
  schedule:
    # 每天 5 次运行（北京时间大致对应：早8点, 午13点, 晚18点, 晚20:30, 凌晨0点）
    - cron: '0 0 * * *'
    - cron: '0 5 * * *'
    - cron: '0 10 * * *'
    - cron: '30 12 * * *'
    - cron: '0 16 * * *'
  workflow_dispatch:      # 允许手动运行

jobs:
  daily_analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          # 👇【核心改良1】强制安装全套美股数据工具，防止数据读取失败
          pip install yfinance pandas_datareader requests urllib3 --upgrade --no-cache-dir

      - name: Run analysis
        env:
          # --- 核心密钥配置 ---
          LLM_PROVIDER: ${{ secrets.LLM_PROVIDER }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          STOCK_LIST: ${{ secrets.STOCK_LIST }}
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
          
          # 👇【核心改良2】强制指定“抗揍”模型
          # 解决 429 限流报错的关键！Flash 模型免费额度高，稳如老狗。
          LLM_MODEL: gemini-1.5-flash
          
          # 👇【核心改良3】无死角兼容
          # 不管程序怎么查 Key，都给它塞进去，防止报“未配置”
          GOOGLE_API_KEY: ${{ secrets.LLM_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.LLM_API_KEY }}
          
        run: python main.py
