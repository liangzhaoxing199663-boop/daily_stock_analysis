name: 'Daily Stock Analysis'

on:
  push:
    branches:
      - master
      - main
  schedule:
    # 每天 5 次
    - cron: '0 0 * * *'
    - cron: '0 5 * * *'
    - cron: '0 10 * * *'
    - cron: '30 12 * * *'
    - cron: '0 16 * * *'
  workflow_dispatch:

jobs:
  daily_analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          # 必装：美股数据包
          pip install yfinance pandas_datareader requests urllib3 --upgrade --no-cache-dir

      # 👇👇👇【第一刀】强行换模型 (确保是 1.5-flash) 👇👇👇
      - name: 🔧 Force Model to 1.5-Flash
        run: |
          grep -rl "gemini-3-flash-preview" . | xargs sed -i 's/gemini-3-flash-preview/gemini-1.5-flash/g' || true
          grep -rl "gemini-2.5-flash" . | xargs sed -i 's/gemini-2.5-flash/gemini-1.5-flash/g' || true

      # 👇👇👇【第二刀】强行降速 (把并发数从 3 改成 1) 👇👇👇
      # 这是解决 429 限流的最终杀手锏！排队一个一个做，绝对不堵车。
      - name: 🐢 Limit Concurrency to 1 (Fix 429 Error)
        run: |
          echo "正在把并发数从 3 降到 1..."
          # 找到代码里所有设置并发的地方，统统改成 1
          find . -type f -name "*.py" -print0 | xargs -0 sed -i 's/max_workers=3/max_workers=1/g'
          find . -type f -name "*.py" -print0 | xargs -0 sed -i 's/max_workers = 3/max_workers = 1/g'
          echo "降速完成！现在是单线程排队模式。"

      - name: Run analysis
        env:
          LLM_PROVIDER: ${{ secrets.LLM_PROVIDER }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          STOCK_LIST: ${{ secrets.STOCK_LIST }}
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
          
          # 环境变量双重锁定
          LLM_MODEL: gemini-1.5-flash
          GOOGLE_API_KEY: ${{ secrets.LLM_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.LLM_API_KEY }}
          
        run: python main.py
